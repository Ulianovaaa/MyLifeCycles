<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MyLifeCycles" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="colorAccent">@color/hot_pink</item>
        <item name="colorPrimary">@color/hot_pink</item>
        <item name="fontFamily">@font/poppins</item>
    </style>

    <style name="Theme.MyLifeCycles" parent="Base.Theme.MyLifeCycles" />
</resources>